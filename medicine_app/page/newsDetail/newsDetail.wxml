<view class="page">
    <view class="head">
        <text class="title1">{{listData.title}}\n</text>
        <text class="date">{{listData.date}}</text>
    </view>
    <image src="{{listData.imgUrl}}"  style="padding-left : 40rpx ; width: {{imageWidth - 40}}px; height: {{imageHeight - 40}}px;" bindload="imageLoad"></image>
    <view wx:for="{{listData.content}}" class="content">
        <view class="title2">{{item.title}}：</view>
        <view style="margin-bottom:20rpx;" wx:for="{{item.des}}" wx:for-item="str">{{str}}</view>
        <image wx:if="{{item.image}}" src="{{item.image}}" style="padding-left : 40rpx ; width: {{imageWidth - 40}}px; height: {{imageHeight - 40}}px;" bindload="imageLoad"></image>
    </view>
</view>   